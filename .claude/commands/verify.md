---
description: Run the build-validator subagent to check build, vet, tests, and lint on the current tree.
---

Launch the `build-validator` subagent. Return its verdict verbatim — do not re-run the checks yourself.
